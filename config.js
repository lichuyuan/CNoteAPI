const config = {
    // session 对称加密 secret key
    secretKey: 'sadsadf',

    // 上传文件目录
    uploadPath: 'uploads/',

    // mongodb address
    db: 'mongodb://localhost:27017/todo',
    
    port: 5000,

    host: '0.0.0.0',
}

module.exports = config
